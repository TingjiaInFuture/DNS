# 配置文件: config.txt (！！格式示例！勿动！)

# 缓存大小限制 (单位：条目数)
cache_size=100

# 日志级别: 可选值为 error, warn, info, debug
log_level=debug

# DNS 服务器监听的端口 (模拟)
server_port=53

# DNS 数据库文件路径
dns_db_file=dns_db.txt

# 外部DNS服务器的地址
external_dns_server= 10.3.9.4
